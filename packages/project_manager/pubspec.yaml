name: project_manager
description: "FL IDE — project creation and management"
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  terminal_pkg:
    path: ../terminal_pkg
  sdk_manager:
    path: ../sdk_manager
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  google_fonts: ^8.0.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
