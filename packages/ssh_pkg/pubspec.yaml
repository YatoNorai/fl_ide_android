name: ssh_pkg
description: SSH remote connection for FL IDE
version: 0.0.1

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  dartssh2: ^2.9.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  path: ^1.9.0
